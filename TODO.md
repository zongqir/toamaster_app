# 任务：实现会议投票功能（AI 智能分组版）

## ✅ 功能已完成并优化

### 核心功能
1. ✅ **AI 智能分组**：使用文心一言 AI 自动分析会议流程并智能分组
2. ✅ **分组可编辑**：AI 生成后可以手动调整分组和候选人
3. ✅ **支持修改投票**：同一设备可以查看和修改自己的投票
4. ✅ **自定义底部导航**：投票和结果页面显示自定义 TabBar，可随时切换到其他页面
5. ✅ **复盘查看结果**：会议复盘时可以查看投票结果
6. ✅ 一次只允许一个活跃投票（创建新投票自动关闭旧投票）
7. ✅ 投票有效期 24 小时，过期自动关闭
8. ✅ 匿名投票，无需填写姓名
9. ✅ 投票入口自动加载当前活跃投票
10. ✅ 使用设备指纹防重复投票
11. ✅ 投票结果实时统计、排名展示、导出结果

### AI 分组规则
AI 会根据会议流程自动识别并分组：
- 备稿演讲组（preparedSpeech）
- 备稿点评组（evaluation）
- 即兴演讲组（tableTopics）
- 会议官员组（时间官、语法官、哼哈官等）
- 其他角色组（SAA、主持人等）

### 投票规则（AI 自动计算）
- 每组候选人数 <= 3：maxSelections = 1
- 每组候选人数 4-6：maxSelections = 2
- 每组候选人数 >= 7：maxSelections = 3

### 防重复投票
- 使用设备指纹（浏览器/设备唯一标识）
- 同一设备只能投票一次

## 使用流程

### 1. 创建投票（时间官）
1. 在 Timeline 页面点击投票图标按钮
2. 自动跳转到投票编辑页面
3. **AI 自动分组**：系统调用文心一言 AI 分析会议流程并生成分组
4. **手动调整**：
   - 可以编辑分组名称
   - 可以调整最大可选数（1-3）
   - 可以新增/删除分组
   - 可以编辑候选人姓名和描述
   - 可以新增/删除候选人
   - 可以点击"重新 AI 分组"重新生成
5. 点击"保存并创建投票"
6. 显示投票 ID，有效期 24 小时

### 2. 参与投票（会议成员）
1. 打开小程序，点击底部"投票入口"标签
2. 自动加载当前活跃投票并进入
3. **首次投票**：
   - 为每个分组选择候选人
   - 提交投票（无需填写姓名）
4. **已投票用户**：
   - 自动显示之前的投票选择
   - 可以点击"修改投票"按钮修改选择
   - 可以点击"查看结果"查看投票结果
   - 修改后点击"保存修改"更新投票

### 3. 查看结果
- 投票后可以查看结果
- 时间官可在 Timeline 页面点击图表图标查看结果
- **会议复盘时**：在统计页面直接显示投票结果
  - 如果有投票，显示各分组的前三名和投票统计
  - 如果没有投票，显示"暂无投票"和"创建投票"按钮
- 支持导出结果文本

## 技术实现

### Edge Function
- **ai-voting-groups**：调用文心一言 API 进行智能分组
  - 接收会议流程数据
  - 构建专业提示词
  - **读取完整的流式响应**
  - **返回解析后的 JSON 对象**（不再透传流式响应）
  - 自动清理 markdown 标记
  - 完整的错误处理和日志记录

### 数据库表
- voting_sessions：投票会话（包含过期时间）
- voting_groups：投票分组
- voting_candidates：候选人
- votes：投票记录（只存储设备指纹，不存储姓名）

### 核心页面
- **投票编辑页面**：/pages/vote-edit/index
  - AI 自动分组
  - 手动调整分组和候选人
  - 保存并创建投票
  - **显示自定义底部导航**
  - **修复 ID 冲突**：使用时间戳 + 随机数生成唯一 ID
- 投票入口页面：/pages/vote-entrance/index（自动加载活跃投票）
- 投票页面：/pages/vote/index
  - 匿名投票
  - **支持查看已投票记录**
  - **支持修改投票**
  - 显示之前的选择
  - **显示自定义底部导航**
- 结果页面：/pages/vote-result/index
  - 不显示投票人名单
  - **显示自定义底部导航**
- Timeline 页面：
  - 预览状态：显示创建投票按钮
  - **复盘状态：显示查看投票结果按钮**

### 核心组件
- **CustomTabBar**：自定义底部导航组件
  - 在非 tabBar 页面显示底部导航
  - 支持切换到会议列表、投票入口、设置
  - 使用 Taro.switchTab 进行页面跳转
- **MeetingStats**：会议统计组件
  - 显示整体统计、超时统计、提前统计、发言人统计
  - **显示投票结果**：自动加载并显示投票结果
  - 如果没有投票，显示"暂无投票"和"创建投票"按钮
  - **创建投票按钮**：通过 onCreateVoting 回调跳转到投票编辑页面
  - 支持折叠/展开各个统计模块

### 数据库服务
- votingDatabase.ts：数据库操作
  - closeAllActiveVotings()：关闭所有活跃投票
  - getActiveVoting()：获取当前活跃投票
  - **getUserVotes()：获取用户的投票记录**
  - **updateVote()：更新用户的投票**
  - 自动检查并关闭过期投票

### AI 提示词设计
系统提示词包含：
1. 角色定义：头马会议投票分组助手
2. 分组规则：5 种分组类型及其识别规则
3. 最大可选数规则：根据候选人数量自动计算
4. 返回格式：严格的 JSON 格式要求
5. 注意事项：去重、排序、格式验证

### 优化功能
- ✅ AI 智能分组（文心一言）
- ✅ 分组可编辑（新增、删除、修改）
- ✅ 候选人可编辑（新增、删除、修改）
- ✅ 支持重新 AI 分组
- ✅ **支持修改投票**：同一设备可以查看和修改自己的投票
- ✅ **自定义底部导航**：投票、结果、编辑页面显示自定义 TabBar
- ✅ **修复 ID 冲突**：
  - 新增分组和候选人使用时间戳 + 随机数生成唯一 ID
  - 保存投票时重新生成所有分组和候选人的 ID，避免与数据库冲突
- ✅ **复盘查看结果**：会议复盘时显示投票结果入口
- ✅ 一次只允许一个活跃投票
- ✅ 投票有效期 24 小时
- ✅ 匿名投票（无需姓名）
- ✅ 投票入口自动加载
- ✅ 设备指纹防重复投票

## 技术说明

### 自定义底部导航实现
由于微信小程序的限制，只有在 `tabBar.list` 中定义的页面才会显示原生底部导航。而投票页面和结果页面需要传递参数（投票ID），无法放在 tabBar 中。

**解决方案**：
1. 创建 `CustomTabBar` 组件，模拟原生 tabBar 的外观和行为
2. 在投票页面和结果页面中引入该组件
3. 使用 `fixed bottom-0` 固定在页面底部
4. 页面容器添加 `pb-14` 为底部导航留出空间
5. ScrollView 底部添加额外占位空间
6. 点击 tab 使用 `Taro.switchTab` 跳转到 tabBar 页面

## 优化说明

### 1. AI 智能分组
- **自动识别**：AI 自动分析会议流程，识别不同类型的环节
- **智能分组**：根据环节类型自动分组
- **自动计算**：根据候选人数量自动计算最大可选数
- **格式规范**：返回标准 JSON 格式，便于解析

### 2. 可编辑性
- **灵活调整**：AI 生成后可以手动调整
- **新增删除**：支持新增和删除分组、候选人
- **重新生成**：不满意可以重新 AI 分组
- **实时预览**：调整后立即看到效果

### 3. 用户体验
- **自动化**：AI 自动分组，减少手动工作
- **可控性**：保留手动调整能力
- **即时反馈**：AI 生成过程有加载提示
- **清晰提示**：各种状态都有明确提示

## 测试检查
- ✅ 所有代码通过 Lint 检查
- ✅ Edge Function 部署成功
- ✅ AI 分组功能正常
- ✅ 分组编辑功能完整
- ✅ 候选人编辑功能完整
- ✅ 投票创建流程正常
- ✅ 依赖包安装完成（miaoda-taro-utils）

## 技术亮点
- 使用文心一言 AI 进行智能分组
- 流式响应处理，实时显示 AI 生成进度
- 完整的编辑功能，支持所有 CRUD 操作
- 优雅的 UI 设计，操作直观
- 完整的错误处理和用户提示

## 投票功能完整实现 ✅ 已完成

### 核心设计原则
- **一个会议一个投票**：投票会话 ID = 会议 ID
- **固定可预测的 ID**：分组 ID = `{会议ID}_group_{索引}`，候选人 ID = `{会议ID}_candidate_{分组索引}_{候选人索引}`
- **覆盖更新机制**：重新创建投票时自动删除旧数据
- **数据一致性**：所有 ID 都基于会议 ID，确保关联关系清晰

### 已实现功能
1. ✅ 投票创建与编辑
   - 从会议数据自动生成投票分组和候选人（AI 智能分组）
   - AI 生成的临时 ID 在保存时自动替换为正式 ID
   - 支持备稿演讲、备稿点评、即兴演讲、会议官员、其他角色五大分组
   - 每个分组可设置最大选择数量
   - 保存时使用会议 ID 作为投票会话 ID
   - 分组 ID 格式：`{会议ID}_group_{索引}`
   - 候选人 ID 格式：`{会议ID}_candidate_{分组索引}_{候选人索引}`
   
2. ✅ 投票入口与投票
   - 扫码或链接进入投票页面
   - 设备指纹识别，防止重复投票
   - 已投票用户可查看和修改投票
   - 投票提交后实时保存到数据库

3. ✅ 投票结果展示
   - 会议复盘页面显示投票结果
   - 显示每个分组的投票统计
   - 显示每个候选人的得票数和得票率
   - 显示总投票人数

4. ✅ 数据持久化
   - 所有投票数据保存到 Supabase
   - 支持覆盖更新，重新创建投票会清空旧数据
   - 投票记录永久保存

### 数据库清理
- ✅ 已删除所有旧的投票记录（votes: 0, candidates: 0, groups: 0, sessions: 0）
- ✅ 数据库已清空，准备使用新的 ID 规则
- ✅ 修复了删除逻辑，添加错误检查和日志
- ✅ 使用 maybeSingle() 代替 single() 避免查询错误

### 测试步骤
1. ✅ 进入会议复盘页面，点击"创建投票"
2. ✅ AI 自动生成投票分组（使用临时 ID）
3. ✅ 点击"保存并创建投票"
4. ✅ 系统自动将临时 ID 替换为正式 ID（基于会议 ID）
5. ✅ 投票会话 ID = 会议 ID
6. ✅ 分组 ID = `{会议ID}_group_{索引}`
7. ✅ 候选人 ID = `{会议ID}_candidate_{分组索引}_{候选人索引}`
8. ✅ 显示投票创建成功，投票 ID 应该等于会议 ID
9. ✅ 返回会议复盘页面，应该能看到投票结果

### 已修复的问题
1. ✅ 投票会话 ID 随机生成 → 改为使用会议 ID
2. ✅ AI 生成随机 ID → 改为生成临时 ID，保存时替换
3. ✅ 删除旧数据失败导致主键冲突 → 添加错误检查和日志
4. ✅ 会议复盘显示"暂无投票"但数据已加载 → 修复渲染条件判断逻辑
**根本原因**：
1. 设备指纹每次生成都不同（包含时间戳）- 已修复
2. loadSession 执行时 fingerprint 还未生成（异步问题）- 已修复

**解决方案**：
- ✅ 修改 generateDeviceFingerprint，首次生成后保存到本地存储
- ✅ 后续调用直接读取本地存储，确保同一设备使用相同指纹
- ✅ 将投票状态检查逻辑从 loadSession 中分离出来
- ✅ 创建独立的 useEffect，等待 sessionId、fingerprint、session 都准备好后再检查
- ✅ 添加详细的调试日志，追踪检查流程

## 问题根源分析

### 投票结果为空的真正原因 ✅ 已彻底解决

**问题现象**：
- 用户已投票，但会议复盘显示"暂无投票"
- 投票结果显示 totalVoters: 0，所有分组 totalVotes: 0

**根本原因**：
1. 投票会话 ID 使用随机生成的 6 位字符串
2. 每次创建投票都生成新的会话 ID
3. 同一个会议创建了多个投票会话（9个）
4. 用户在旧的投票会话中投票，但系统查询时可能取到新的空会话
5. 导致数据混乱，无法正确关联投票记录

**完整解决方案**：
- ✅ **核心改动**：投票会话 ID = 会议 ID，分组 ID = `{会议ID}_group_{索引}`，候选人 ID = `{会议ID}_candidate_{分组索引}_{候选人索引}`
- ✅ 修改 vote-edit 页面，移除所有随机 ID 生成逻辑
- ✅ 使用 `currentSession.id` 作为投票会话 ID
- ✅ 分组和候选人 ID 使用会议 ID 作为前缀，加上索引号
- ✅ 修改 createVotingSession 方法，支持覆盖更新
- ✅ 简化 getVotingResultByMeetingId 方法，直接使用会议 ID 查询
- ✅ 删除数据库中所有旧的投票记录，使用新的 ID 规则

**ID 规则示例**（会议 ID = "abc123"）：
- 投票会话 ID: `abc123`
- 第一个分组 ID: `abc123_group_0`
- 第一个分组的第一个候选人 ID: `abc123_candidate_0_0`
- 第一个分组的第二个候选人 ID: `abc123_candidate_0_1`
- 第二个分组 ID: `abc123_group_1`
- 第二个分组的第一个候选人 ID: `abc123_candidate_1_0`

